# The repo is published as a public website: everything committed here is
# downloadable by anyone. Master files live OUTSIDE the repo, in
# ../my/srijana_work/20260812/github_io_webpage/ORIGINALS/ — see its README.txt.

# Personal documents must never be committed (tax letters, ID scans, invoices).
*.pdf
!docs/*.pdf

# Camera originals dropped into images/ by mistake — the site serves the
# versions generated into images/web, images/thumbs and images/events.
images/IMG_*.jpg
images/IMG_*.JPG
images/*.mov
images/*.MOV

.DS_Store
**/.DS_Store
